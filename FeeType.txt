
I have a below interface in my spring boot application which I am autowiring in DealStructureService class which I have given below, 
I am gettting error saying DealHandlerClient bean can't be created please consider creation a bean in  DealStructureService, why is it giving error? despite of @Service internally AUtowires the DealHandlerClient object?



import com.cacib.loanscape.types.DealInput;
import com.cacib.loanscape.types.DealStatusInput;
import com.cacib.loanscape.types.report.Response;
import feign.Headers;
import feign.Param;
import feign.RequestLine;
import jakarta.validation.Valid;

@Headers({"Content-Type: application/vnd.od.api.v1+json;charset=UTF-8"})
public interface DealHandlerClient {
  @RequestLine("POST /api/v1/deals")
  @Headers({"Authorization: {authHeader}", "X-Correlation-Id: {correlationId}"})
  Response createDeal(@Param("authHeader") String authHeader, @Param("correlationId") String correlationId, @Valid DealInput input);

  @RequestLine("PUT /api/v1/deals/{functionalId}/{functionalVersion}")
  @Headers({"Authorization: {authHeader}", "X-Correlation-Id: {correlationId}"})
  Response updateDeal(@Param("authHeader") String authHeader, @Param("correlationId") String correlationId, @Param("functionalId") String functionalId, @Param("functionalVersion") int functionalVersion, @Valid DealInput input);

  @RequestLine("PUT /api/v1/deals/{functionalId}/{functionalVersion}/updatestatus")
  @Headers({"Authorization: {authHeader}", "X-Correlation-Id: {correlationId}"})
  Response updateDealStatus(@Param("authHeader") String authHeader, @Param("correlationId") String correlationId, @Param("functionalId") String functionalId, @Param("functionalVersion") int functionalVersion, @Valid DealStatusInput input);
}


@Service
@RequiredArgsConstructor
@ConditionalOnProperty(name = "od.handler.enabled", havingValue = "true")
@Slf4j
public class DealStructureService implements IDealStructureService {
  private final DealHandlerClient dealHandlerClient;
  
 }


